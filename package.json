{
  "name": "process-monitoring-tool-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend tests for the Process Monitor dashboard. The shipped UI itself remains dependency-free; only the test harness needs Node packages.",
  "scripts": {
    "test": "node --test ui/tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}

